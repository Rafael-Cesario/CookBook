import { Header } from "@/features/auth/components/header";
import { Main } from "@/features/auth/components/main";

const Auth = () => {
	return (
		<>
			<Header />
			<Main />
		</>
	);
};

export default Auth;
